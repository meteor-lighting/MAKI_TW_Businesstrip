# 導覽頁面與歷史資料查詢系統 實作計畫

本計畫目標是：
1. **導覽入口**：在使用者登入後，進入一個過渡的導覽大廳（Home頁面），提供「我的報告」與「歷史資料」兩個入口。
2. **歷史查詢**：提供一個可跨多份差旅報告、快速篩選歷史細項花費（例如篩出某員工的歷史機票花費）的進階查詢介面。

> [!IMPORTANT]
> **使用者回覆需求**
> 在這份計畫的「功能細節決定」區域（步驟三）有針對**「歷史請款紀錄顯示方式」**提出兩種 UX 方案。請您先閱讀並挑選出比較符合您預期的顯示方式再繼續。

---

## 一、新增登入後大廳頁 (Home Screen)

登入後不再直接跳轉進入 `Dashboard`（我的報告列表），而是跳轉至新的 `/home` 路由。

**畫面設計：**
- 採簡潔的全螢幕雙區塊（分割 / 兩張卡片）佈局。
- 左側選項/卡片：**「我的報告 (My Reports)」** -> 點擊後跳轉至現有的 `/dashboard`。
- 右側選項/卡片：**「歷史資料查詢 (Historical Data)」** -> 點擊後跳轉至即將實作的 `/history` 頁面。

---

## 二、過濾條件介面 (Filter UI) 設計

進入 `/history` 頁面後，畫面頂部（或左側過濾邊欄）會提供以下篩選欄位：

1. **員工編號** (Input 輸入框)：輸入員工編號。如果是普通權限用戶，或許自動鎖死為自己的編號，若是管理員則可自由輸入（或下拉選擇）。
2. **品項 / 分類** (Select 下拉選單)：依據您的 Gas 表單設計，會有：`全部 (All)`、`機票`、`住宿`、`租車`... 等等。
3. **出差國家/地點** (Input 或 Autocomplete)：對應 `Report Header` 中的出差地點。
4. **差旅報告名稱** (Input 輸入框)：模糊搜尋報告名稱。
5. **查詢按鈕** (Button)。

---

## 三、歷史請款紀錄列表設計與技術做法 (需您抉擇)

由於差旅開銷的「每一個品項」格式差異極大（如：機票有航班與跨日，住宿卻是幾人入住、退房時間）。我們如果要把結果羅列成「歷史表格」，有兩種常見的方法：

### 方案 A：依據「所選品項」動態切換表格欄位（推薦 ✅）
- **操作方式**：當使用者在篩選條件中，**強制指定了一個品項**（例如「機票」）。
- **畫面呈現**：下方會彈出我們之前在寫報告時用過的「機票 DataGrid 專用表格」（含出發地、航班、抵達時間等專屬欄位）。這每一筆紀錄會附加它是來自哪一張報告（以及報告名稱、該員工名稱）。
- **如果是選「全部」**：若品項選全部，下方只能單純列出「符合條件的報告總表」（就像目前 Dashboard 長得那樣），或是改為列出一個統一的簡化版紀錄（只有：日期、單據金額、幣別、分類四個共用屬性）。

### 方案 B：統一的泛用紀錄列表
- **操作方式**：無論選擇哪種品項過濾器。
- **畫面呈現**：所有請款紀錄被扁平化為統一欄位。例：`[報告名稱] | [員工編號] | [日期] | [分類/品項] | [金額] | [幣別] | [備註]`。
- **優缺點**：好處是可以把機票、住宿、計程車混在同一個表格內一起排序或加總；缺點是使用者看不到「機票的航班」或「住宿哪間飯店」等詳細資料，若要看細節，必須給一個「檢視報告」的按鈕連回整張報表裡面。

> [!QUESTION]
> **您的選擇是？**
> 請告訴我您傾向 **【方案 A (會呈現品項專屬表單欄位)】** 還是 **【方案 B (僅顯示金額幣別等通用的扁平資訊)】** 以便我進行實作？

---

## 四、技術與後端調整

1. **前端 Route**：新增 `/home` 及 `/history` 路由。
2. **後端 API (`gas/Code.gs`)**：新增一個名為 `queryHistoricalData(filters)` 的 API。
   - 運作時，會在內部先抓取 `Report Header` 篩選「報告名稱」、「員工編號」、「出差地點」。
   - 然後根據符合的「報告編號 ID」，針對 `filters.category` (品項) 的資料表進行交叉 Filter。
   - 最後組成 JSON 陣列打包丟回前端。
3. **前端元件化**：我們會藉機重用之前已經刻好的那些 DataGrid 表格顯示層，確保介面風格統一。
